@page "/orders/details"
@using Microsoft.EntityFrameworkCore
@using DataAccess
@using SengWeb.Data
@inject IDbContextFactory<SengWebContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Order Details</PageTitle>

<h1>Order Details</h1>

@if (order is null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div>
        <h2>Order #@order.OrderID</h2>
        <hr />

        <dl class="row">
            <dt class="col-sm-2">Customer</dt>
            <dd class="col-sm-10">@(order.Customer?.Name ?? "No Customer")</dd>

            <dt class="col-sm-2">Employee</dt>
            <dd class="col-sm-10">@(order.Employee?.Name ?? "No Employee")</dd>

            <dt class="col-sm-2">Date</dt>
            <dd class="col-sm-10">@order.Date</dd>

            <dt class="col-sm-2">Total Amount</dt>
            <dd class="col-sm-10">@order.TotalAmount</dd>

            <dt class="col-sm-2">Last Modified</dt>
            <dd class="col-sm-10">@order.LastModified</dd>
        </dl>

        <h3>Products in this Order</h3>

        @if (orderDetails.Count == 0)
        {
            <p>No products have been added to this order yet.</p>
        }
        else
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>Quantity</th>
                        <th>Subtotal</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var detail in orderDetails)
                    {
                        <tr>
                            <td>@(detail.Product?.Name ?? "No Product")</td>
                            <td>@detail.Quantity</td>
                            <td>@detail.Subtotal</td>
                            <td>
                                <a href="@($"/orderdetails/edit?orderid={detail.OrderID}&productid={detail.ProductID}")">Edit</a>
                                |
                                <a href="@($"/orderdetails/delete?orderid={detail.OrderID}&productid={detail.ProductID}")">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }

        <div>
            <a href="@($"/orderdetails/create?orderid={order.OrderID}")" class="btn btn-primary">Add Product</a>
            <a href="@($"/orders/edit?orderid={order.OrderID}")" class="btn btn-secondary">Edit Order</a>
            <a href="/orders" class="btn btn-link">Back to List</a>
        </div>
    </div>
}

@code {
    private Order? order;
    private List<OrderDetail> orderDetails = new();

    [SupplyParameterFromQuery]
    private int OrderID { get; set; }

    protected override async Task OnInitializedAsync()
    {
        using var context = DbFactory.CreateDbContext();

        order = await context.Order
            .Include(o => o.Customer)
            .Include(o => o.Employee)
            .FirstOrDefaultAsync(o => o.OrderID == OrderID);

        if (order is null)
        {
            NavigationManager.NavigateTo("notfound");
            return;
        }

        orderDetails = await context.OrderDetail
            .Include(od => od.Product)
            .Where(od => od.OrderID == OrderID)
            .ToListAsync();
    }
}